final: prev: {
  # Exemple d'overlay (à décommenter/adapter si besoin) :
  # mon-paquet-patche = prev.mon-paquet.overrideAttrs (old: {
  #   patches = (old.patches or []) ++ [ ./patches/mon-patch.diff ];
  # });
}